// Package types defines the public API types for brainkit.
// This package is the leaf of the dependency tree — it imports nothing
// from brainkit. Both the root facade and internal/engine import it.
package types
