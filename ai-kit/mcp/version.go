// Ported from: packages/mcp/src/version.ts
package mcp

// VERSION is the package version string.
// In the TS source this is injected at build time via __PACKAGE_VERSION__.
const VERSION = "0.0.0-go"
