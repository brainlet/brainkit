# Browser Fixtures

Fixtures in this category use `modules/browser`.

Current coverage proves Brainkit's browser lifecycle control surface is
reachable from deployed TypeScript. It does not claim concrete Mastra browser
provider support. Provider rows still require real provider packages, real
browser/CDP lifecycle, live model/browser behavior where applicable, and
hot-unmount cleanup proof.
