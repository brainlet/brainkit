

# Package

- Should deploy one file from path as a package (if valid)
- Should deploy one folder from path as a package (if valid)
- Should deploy one file from string as a package using a message (if valid)
- Should deploy one folder from strings as a package using a message (if valid)

